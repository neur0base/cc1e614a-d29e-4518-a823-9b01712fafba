import { RoutingConfig } from "@neur0base/app-sdk-core";

const routing: RoutingConfig[] = [
    {
        "id": "MyTasks",
        "name": "MyTasks",
        "device": "both"
    }
];

export default routing;
